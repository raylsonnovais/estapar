spring:
  datasource:
    url: jdbc:mysql://localhost:3306/parking_test?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: parking
    password: parking
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    enabled: true

garage:
  simulator:
    base-url: http://localhost:${wiremock.server.port:8089}
